services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PHP_VERSION: ${PHP_VERSION:-8.5}
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
